[package]
name = "mamo-sim"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[dev-dependencies]
# No extra deps needed — unit tests run on native target

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
